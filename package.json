{
  "name": "unicure-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "cd unicure && npm run build",
    "dev": "cd unicure && npm run dev",
    "preview": "cd unicure && npm run preview"
  }
}